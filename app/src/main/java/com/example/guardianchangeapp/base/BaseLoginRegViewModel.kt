package com.example.guardianchangeapp.base

import android.app.Application
import androidx.lifecycle.AndroidViewModel

abstract class BaseLoginRegViewModel(application: Application) : AndroidViewModel(application) {
}